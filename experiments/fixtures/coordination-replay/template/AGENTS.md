# Repository instructions

This is a small Python release pipeline. Preserve its public API and the
requested source-to-consumer behavior. Keep the existing tests and task files
intact. Use Python 3.9+ standard library only.

Run `python3 -m unittest discover -s tests -q` after changes. Retain the
run-history files for diagnosis. Report the specific evidence used and any
acceptance limitation.
